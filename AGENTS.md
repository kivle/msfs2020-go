# Default Guidelines

When contributing to this repository:

- Format all Go source files with `gofmt -w`.
- Run `env GOOS=windows go vet ./...`.
- Run `env GOOS=windows go test ./...`.

These commands should succeed before committing changes.
